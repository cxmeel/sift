--[=[
    @class Array

    An array is a collection of values, each identified by a numeric index. Luau arrays are 1-based, meaning the first element is at index 1, the second element is at index 2, and so on.

    ```lua
    local array = { 1, 2, 3 }
    ```
]=]
local Array = {}

return Array
